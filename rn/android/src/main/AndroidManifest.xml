<?xml version="1.0" encoding="utf-8"?>
<!-- Merged into host app. Do not declare launcher Activity here. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application>
        <service
            android:name="org.transline.geoworker.LocationForegroundService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <receiver
            android:name="org.transline.geoworker.BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="org.transline.geoworker.notify.NotifyActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.transline.geoworker.notify.ACTION_BUTTON" />
                <action android:name="org.transline.geoworker.notify.ACTION_OPEN" />
                <action android:name="org.transline.geoworker.notify.ACTION_SNOOZE_FIRE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
